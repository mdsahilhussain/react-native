import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Favorites_Screen = () => {
  return (
    <View>
      <Text>favorites_screen</Text>
    </View>
  );
};

export default Favorites_Screen;

const styles = StyleSheet.create({
  screen: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
});
